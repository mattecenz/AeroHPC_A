#include <iostream>

void solver() {
    
    // define T & deltaT

    // define the mesh:
    // instantiate from ghosted stagg grid
    // hint: second method, num of nodes , gh=1

    // build the model
    // initialize the grid with initial values


    while (currentTime < T){
        // apply boundary cond
        // call RK
        currentTime += deltaT
    }
}

int main() {
    
    return 0;
}